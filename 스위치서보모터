// C++ code
//

#include <Servo.h>

Servo servo;

void setup()
{ 
  pinMode(8, INPUT_PULLUP);
  servo.attach(4);
}

void loop()
{
  if(digitalRead(8)==LOW) // 스위치 누름
  {
    servo.write(90); // 직각으로 올라감
    delay(1000);
  }
  else
  {
    servo.write(0); // 수평으로 내려감
    delay(1000);
  }
}
